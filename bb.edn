{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([ncr_retalix.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'ncr_retalix.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
